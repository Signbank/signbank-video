============
Installation
============

At the command line::

    $ easy_install signbank-video

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv signbank-video
    $ pip install signbank-video
